{% if menu %}
	<ul>
	{% for item in menu %}
		<li class="{{item.classes | join(' ')}} nav">
			<a href="{{item.get_link}}" class="nav-item">{{item.title}}</a>
			{% include "menu.twig" with {'menu': item.get_children} %}
		</li>
	{% endfor %}
	</ul>
{% endif %}
