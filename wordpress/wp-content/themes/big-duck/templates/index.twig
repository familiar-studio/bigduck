{% extends "base.twig" %}

{% block content %}
	<div class="container">
		<div id="global-app">
			${ msg }
			<div v-for="(value, field) in acf">
				<div>ACF handle: ${ field }</div>
				<div>value: ${ value }</div>
			</div>
			<main-component>

			</main-component>
		{# {% for post in posts %}
			{% include ['tease-'~post.post_type~'.twig', 'tease.twig'] %}
		{% endfor %} #}
		</div>
	</div>
{% endblock %}
